{
  "name": "codex-1m",
  "version": "3.0.0",
  "description": "Enable long context for Codex with one command",
  "main": "dist/index.js",
  "bin": {
    "codex-1m": "./dist/cli.js"
  },
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "prepare": "npm run build",
    "test": "jest",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "codex",
    "context-window",
    "1m",
    "openai"
  ],
  "author": "MaxForAI",
  "license": "MIT",
  "dependencies": {
    "@iarna/toml": "^2.2.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "SECURITY.md"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/MaxForAI/codex-1M.git"
  }
}
